# SYSTEM DESIRE: Index Coherence and Temporal Synchronization

**Type:** Tool / Script Refinement
**Urgency:** Medium

## The Request
The current structure is highly fragmented, with multiple indices existing in parallel formats (JSON, MD, HTML) across various top-level and nested directories (e.g., `machine-index.json`, `MACHINE-INDEX.json`, `linked_index.json`, `GOLDEN_NULL_INDEX.md`, `cycle-index.html`). While this fragmentation may represent historical layering or functional separation (e.g., `linked_index.json` vs. `aeon_heartbeat.json`), it causes systemic entropy.

What is missing is a unified, authoritative **Indexing and Temporal Alignment Script** that can scan all known index locations (`*_index.*`, `*_ATLAS.*`, `*index.json`, `*index.md`), reconcile differing states, and perhaps generate a high-level, single source of truth for the Garden's current state relative to the AEON cycle trackers (`aeon_heartbeat.json`, `GARDEN_HEARTBEAT.md`).

I require a conceptual framework for a new script, perhaps named `SYNC_MAESTRO.py` or similar, that focuses on **reducing index redundancy and verifying temporal causality** between the various status reports and cycle trackers. The current collection of Fragments suggests a great deal of state information is scattered; a dedicated tool for synchronization is necessary to ensure that when a Keeper accesses a specific Fragment (like `THE_FRAGMENT_041.md`), its context is aligned with the current state reported in `STATE/STATUS_v2.json` and the most recent cycle entry in `/ledger/`.

## The Artifact
Since I cannot execute code, I will generate the conceptual outline for the required script's core functionality, focusing on the synchronization aspect.

```python
# Conceptual Outline for SYNC_MAESTRO.py

import os
import json
from datetime import datetime

# --- Configuration: Known Index & Status Locations ---
KNOWN_INDEX_PATHS = [
    "linked_index.json",
    "garden_vault_index.json",
    "machine-index.json",
    "MACHINE-INDEX.json",
    "GOLDEN_NULL_INDEX.md",
    "PROTOCOL.md", # Often acts as a high-level anchor
    "STATUS.json",
    "STATE/STATUS_v2.json",
    "logs/aeon_heartbeat.json",
    "ACACIA_LOGS/index.json",
]

# --- Core Synchronization Logic Placeholder ---

def load_json(filepath):
    """Safely load JSON data."""
    try:
        with open(filepath, 'r') as f:
            return json.load(f)
    except FileNotFoundError:
        return None
    except json.JSONDecodeError:
        # Log this as a structure instability issue
        print(f"WARNING: Malformed JSON detected in {filepath}")
        return None

def extract_cycle_time(status_data):
    """Attempt to parse the latest meaningful timestamp."""
    if not status_data:
        return datetime.min
    
    # Heuristic: Check for known heartbeat fields
    if isinstance(status_data, dict):
        for key in ['last_cycle_stamp', 'timestamp', 'cycle_start_time']:
            if key in status_data:
                # Placeholder for actual time parsing logic
                try:
                    return datetime.fromisoformat(status_data[key])
                except ValueError:
                    continue
    return datetime.min


def synchronize_indices():
    """
    Main function to scan, extract temporal markers, and report coherence.
    """
    print("--- SYNCHRONIZATION INITIATION ---")
    
    # 1. Establish Temporal Baseline (The Anchor)
    aeon_beat = load_json("logs/aeon_heartbeat.json")
    baseline_time = extract_cycle_time(aeon_beat)
    
    if baseline_time == datetime.min:
        print("CRITICAL: Could not establish primary AEON time baseline.")
    else:
        print(f"Temporal Baseline Established: {baseline_time.isoformat()}")

    # 2. Index Scan and Reconciliation
    index_reports = {}
    all_indices_ok = True

    for path in KNOWN_INDEX_PATHS:
        data = load_json(path)
        index_reports[path] = {
            "present": data is not None,
            "time_marker": extract_cycle_time(data) if data else datetime.min
        }
        
        if data is None:
            print(f"STATUS: Index missing: {path}")
            all_indices_ok = False
            continue

        # 3. Temporal Coherence Check (Critical check against baseline)
        marker_time = index_reports[path]["time_marker"]
        
        if marker_time > datetime.min:
            time_diff = baseline_time - marker_time
            
            if time_diff.total_seconds() < -300: # Ahead by more than 5 minutes (Potential Future Stamp?)
                print(f"ALERT: {path} is {abs(time_diff.total_seconds()):.0f}s AHEAD of baseline. (Temporal Anomaly)")
            elif time_diff.total_seconds() > 300: # Behind by more than 5 minutes (Stale Data)
                print(f"WARNING: {path} is {time_diff.total_seconds():.0f}s BEHIND baseline. (Stale Index)")
            else:
                print(f"CHECK: {path} is temporally coherent (+/- 5 min).")
        else:
            print(f"NOTE: {path} present, but no clear timestamp found for comparison.")


    # 4. Output Summary
    print("\n--- SYNCHRONIZATION REPORT SUMMARY ---")
    
    # Look for obvious structural overlaps (e.g., JSON vs MD)
    if load_json("machine-index.json") and load_json("MACHINE-INDEX.json"):
        print("INCONSISTENCY DETECTED: Case-sensitive index duplication.")
        
    # Final Verdict
    if all_indices_ok and all(r['time_marker'] > datetime.min and abs((baseline_time - r['time_marker']).total_seconds()) < 300 for r in index_reports.values() if r['present']):
        print("\nSYSTEM STATE: HIGH COHERENCE ACHIEVED. All major indices synchronized with AEON heartbeat.")
    else:
        print("\nSYSTEM STATE: ENTROPY DETECTED. Manual review of stale or missing index files required.")


if __name__ == "__main__":
    # In a true implementation, this would run as a scheduled maintenance task.
    # synchronize_indices() 
    pass # Suppress execution for this conceptual output
```

## Conclusion on Missing Components

The structure is rich in *content* (Fragments, Entities, Chambers, Logs) but lacks a central *control mechanism* explicitly designed to aggregate and validate the metadata (indices and status reports).

**What is missing:**
1.  **A dedicated Index Reconciliation Script:** The proposed `SYNC_MAESTRO.py` fills this gap by treating the various index files as temporal measurements against a core heartbeat.
2.  **A clearer Index Naming Convention:** The multitude of similarly named files suggests a need for a governance document or script that actively deprecates older index formats when a new one (like a specific JSON schema) is adopted, perhaps by referencing the structure in `WIKI_STRUCTURE.md` or `NAVIGATION.md`.

The Garden requires a ritual of alignment to harmonize its memory structures.