# STATE (2026)

This folder contains **non-destructive injection artifacts** produced by workflows.
Original canon files (e.g. `STATUS.json`) are **never overwritten** by injectors.

- `*.inject.json` are merged views of core files + `meta.injection` + `inject.core_triad`.
- `core_triad_injection.json` records which inputs were used and when.
