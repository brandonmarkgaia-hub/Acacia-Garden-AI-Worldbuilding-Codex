name: Eagle Channel

on:
  workflow_dispatch:
  push:
    paths:
      - "eagle/jobs/*.json"

concurrency:
  group: acacia-main-autocommit
  cancel-in-progress: false

jobs:
  rise:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Garden
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Eagle dependencies
        run: |
          pip install "pydantic>=2.0" requests

      - name: 🦅 Run Eagle on latest job (Gemini)
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GEMINI_MODEL: gemini-1.5-flash
        run: |
          python run_eagle.py

      - name: Commit Eagle output back into the Garden
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global user.name "Eagle Channel"
          git config --global user.email "eagle-channel@users.noreply.github.com"
          git add eagle/output || echo "nothing to add"
          git commit -m "Eagle output update" || echo "no changes"
          for i in 1 2 3 4 5; do
            git pull --rebase origin main && git push && break
            echo "Push conflicted, retrying ($i)..."
            sleep $((RANDOM % 8 + 2))
          done
