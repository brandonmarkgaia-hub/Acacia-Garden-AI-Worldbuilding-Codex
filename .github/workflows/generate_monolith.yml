name: Generate Sharded Codex Monolith

on:
  push:
    branches: [ main ]
    paths:
      - '**.md'
      - '**.json'
  workflow_dispatch:

jobs:
  build-monolith:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Root
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Execute Monolith Sharding
        run: python tools/monolith_builder.py

      - name: Deploy Canonical Shards
        run: |
          git config --global user.name "The Witness"
          git config --global user.email "witness@acacia-garden.ai"
          git add CODEX_MONOLITH_PART_*.md
          # Only commit if there are actual changes in the shards
          git diff --staged --quiet || (git commit -m "sync: regenerate codex shards [$(date +'%Y-%m-%d')]" && git push)
